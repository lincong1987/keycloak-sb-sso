<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jiuxi.admin.core.mapper.CJsonMapper">

    <!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.jiuxi.admin.core.bean.entity.CJson" id="cJsonMap">
        <result property="id" column="id"/>
        <result property="referId" column="refer_id"/>
        <result property="dataDetail" column="data_detail"/>
        <result property="actived" column="ACTIVED"/>
        <result property="creator" column="CREATOR"/>
        <result property="createTime" column="CREATE_TIME"/>
        <result property="updator" column="UPDATOR"/>
        <result property="updateTime" column="UPDATE_TIME"/>
        <result property="extend01" column="EXTEND01"/>
        <result property="extend02" column="EXTEND02"/>
        <result property="extend03" column="EXTEND03"/>
    </resultMap>

    <insert id="add" parameterType="com.jiuxi.admin.core.bean.entity.CJson">
        INSERT INTO ${mcode}
            (id, refer_id, data_detail, ACTIVED, CREATOR, CREATE_TIME, UPDATOR, UPDATE_TIME, EXTEND01, EXTEND02, EXTEND03)
        VALUES
            (#{id}, #{referId}, #{dataDetail}, #{actived}, #{creator}, #{createTime}, #{updator}, #{updateTime}, #{extend01}, #{extend02}, #{extend03})
    </insert>

    <update id="update" parameterType="com.jiuxi.admin.core.bean.entity.CJson">
        UPDATE ${mcode}
        <set>
            <if test="null != dataDetail">data_detail = #{dataDetail},</if>
            <if test="null != actived">ACTIVED = #{actived},</if>
            <if test="null != creator">CREATOR = #{creator},</if>
            <if test="null != createTime">CREATE_TIME = #{createTime},</if>
            <if test="null != updator">UPDATOR = #{updator},</if>
            <if test="null != updateTime">UPDATE_TIME = #{updateTime},</if>
            <if test="null != extend01">EXTEND01 = #{extend01},</if>
            <if test="null != extend02">EXTEND02 = #{extend02},</if>
            <if test="null != extend03">EXTEND03 = #{extend03},</if>
        </set>
        WHERE id = #{id}
    </update>

    <select id="view" parameterType="String" resultType="com.jiuxi.admin.core.bean.vo.CJsonVO">
        SELECT id, refer_id, data_detail, ACTIVED, CREATOR, CREATE_TIME, UPDATOR, UPDATE_TIME, EXTEND01, EXTEND02, EXTEND03 FROM ${mcode}
        WHERE
        ACTIVED = 1
        and
        id = #{id}
    </select>

    <update id="delete" parameterType="String">
        UPDATE ${mcode} SET ACTIVED = 0 WHERE id = #{id}
    </update>

</mapper>
